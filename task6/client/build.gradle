dependencies {
    implementation project(':common')
}

shadowJar {
    archiveBaseName.set("client")
    archiveClassifier.set("")
    archiveVersion.set("")
    destinationDirectory.set(file("$rootDir/build/out"))

    manifest {
        attributes 'Main-Class': 'ru.shift.client.Main'
    }
}