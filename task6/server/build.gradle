dependencies {
    implementation project(':common')
}

shadowJar {
    archiveBaseName.set("server")
    archiveClassifier.set("")
    archiveVersion.set("")
    destinationDirectory.set(file("$rootDir/build/out"))

    manifest {
        attributes 'Main-Class': 'ru.shift.server.Main'
    }
}